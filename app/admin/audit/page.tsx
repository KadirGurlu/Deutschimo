import { requireAdmin } from "@/lib/auth/authorization";import { AdminSidebar } from "@/components/layout/admin-sidebar";import { AuditLogViewer } from "@/components/admin/audit-log-viewer";export default async function Page(){await requireAdmin();return <div className="admin-shell"><AdminSidebar active="audit"/><main className="admin-main"><span className="eyebrow">DENETİM GEÇMİŞİ</span><h1 className="section-title">Yönetici işlemleri</h1><p className="section-copy">Rol, durum, test kullanıcı etiketi ve silme işlemleri değiştirilemez denetim kayıtlarıyla izlenir.</p><section className="panel" style={{marginTop:24}}><AuditLogViewer/></section></main></div>}
