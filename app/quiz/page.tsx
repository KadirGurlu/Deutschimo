"use client";
import { useState } from "react";
import Link from "next/link";
import { CheckCircle2 } from "lucide-react";
import { Progress } from "@/components/ui/progress";

export default function QuizPage(){const [answer,setAnswer]=useState("B");const [checked,setChecked]=useState(true);return <section className="quiz-shell"><div className="quiz-head"><div><span className="eyebrow">A1.1 · ÜNİTE 1</span><strong style={{display:"block",color:"var(--heading)"}}>Kendini Tanıtmak · Mini Quiz</strong></div><span>3 / 5 soru</span></div><Progress value={60}/><article className="quiz-card" style={{marginTop:18}}><span className="level-badge">Çoktan seçmeli</span><h1>“Ben İstanbul'da yaşıyorum.” cümlesinin doğru Almanca karşılığı hangisidir?</h1><div className="answers">{[["A","Ich komme in Istanbul."],["B","Ich wohne in Istanbul."],["C","Ich heiße Istanbul."],["D","Ich studiere Istanbul."]].map(([key,text])=><button onClick={()=>{setAnswer(key);setChecked(false)}} className={`answer ${answer===key?"selected":""}`} key={key}><strong>{key}.</strong> {text}</button>)}</div><button onClick={()=>setChecked(true)} className="button button-primary">Cevabı Kontrol Et</button>{checked?<div className="feedback-box"><div style={{display:"flex",gap:10,alignItems:"center"}}><CheckCircle2 color="var(--success)"/><strong>Doğru cevap: B</strong></div><p><strong>wohnen</strong> bir yerde ikamet etmek/yaşamak anlamına gelir. Şehir adı <strong>in</strong> edatıyla kullanılır: <em>Ich wohne in Istanbul.</em></p><small>İlgili konu: Kendini tanıtma ifadeleri</small></div>:null}<div style={{display:"flex",justifyContent:"space-between",marginTop:26}}><Link className="button button-secondary" href="/learn/kendini-tanitmak">Derse Dön</Link><Link className="button button-primary" href="/dashboard">Sonraki Soru →</Link></div></article></section>}
