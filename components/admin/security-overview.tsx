"use client";import{useEffect,useState}from"react";import{AlertTriangle,Archive,LockKeyhole,TestTube2}from"lucide-react";type Data={errors:number;failures:number;blocked:number;testUsers:number;backupConfigured:boolean;backups:Array<{id:string;status:string;startedAt:string;byteSize:number|null;errorMessage:string|null}>};export function SecurityOverview(){const[data,setData]=useState<Data|null>(null);useEffect(()=>{fetch("/api/admin/security",{cache:"no-store"}).then(r=>r.json()).then(setData)},[]);if(!data)return <div className="learning-loading">Güvenlik verileri yükleniyor…</div>;return <><div className="stats-grid"><S icon={<AlertTriangle/>} label="Sistem hatası · 24 saat" value={data.errors}/><S icon={<LockKeyhole/>} label="Başarısız API · 24 saat" value={data.failures}/><S icon={<LockKeyhole/>} label="Başarısız giriş · 24 saat" value={data.blocked}/><S icon={<TestTube2/>} label="Test kullanıcısı" value={data.testUsers}/></div><section className="panel" style={{marginTop:22}}><h2><Archive size={20}/> Günlük yedekler</h2><p>{data.backupConfigured?"Özel Blob yedekleme yapılandırıldı.":"Yedekleme için projeye bağlı Private Blob ve BACKUP_ENCRYPTION_KEY ayarlanmalı."}</p><div className="audit-list">{data.backups.map(b=><article key={b.id}><div><strong>{b.status}</strong><span>{b.byteSize?`${Math.round(b.byteSize/1024)} KB`:b.errorMessage||"Henüz tamamlanmadı"}</span></div><time>{new Intl.DateTimeFormat("tr-TR",{dateStyle:"medium",timeStyle:"short"}).format(new Date(b.startedAt))}</time></article>)}</div></section></>};function S({icon,label,value}:{icon:React.ReactNode;label:string;value:number}){return <article className="stat-card"><div>{label}{icon}</div><strong>{value}</strong><span>Son 24 saat</span></article>}
